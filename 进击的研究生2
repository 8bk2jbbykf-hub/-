# 《研究生的一天》：《植物实验室生存指南》

这是一个可部署到 Cloudflare Pages 的原生 HTML/CSS/JavaScript 网页小游戏，包含前端游戏、Pages Functions API、D1 排行榜、Turnstile 防刷验证、本地存档和登录/云存档预留接口。

## 你需要上传到 GitHub 的内容

请把项目根目录下这些内容全部上传到 GitHub 仓库根目录：

```text
public
functions
schema.sql
wrangler.toml
README.md
UPLOAD_CHECKLIST.txt
```

上传后，GitHub 仓库首页应该直接看到 `public`、`functions`、`schema.sql`、`wrangler.toml`，不要多套一层“游戏网页制作”文件夹。

## Cloudflare Pages 构建设置

连接 GitHub 仓库后，Cloudflare Pages 使用下面设置：

```text
Framework preset: None
Build command: 留空
Build output directory: public
Root directory: 留空
```

本项目的 `wrangler.toml` 已经是 Pages 后台绑定 D1 的安全版本，不再包含旧版 D1 占位配置。

## D1 数据库设置

1. 在 Cloudflare Dashboard 创建 D1 数据库，名称建议：

```text
graduate-game-db
```

2. 打开 D1 数据库的控制台或 Query 页面。
3. 复制 `schema.sql` 的全部内容并执行。
4. 确认创建了这些表：

```text
leaderboard
users
login_codes
sessions
cloud_saves
rate_limits
```

## 绑定 D1 到 Pages 项目

进入你的 Cloudflare Pages 项目：

```text
Workers 和 Pages → 你的项目 → 设置 → 绑定
```

添加 D1 数据库绑定：

```text
变量名称：DB
D1 数据库：graduate-game-db
```

注意：变量名称必须是 `DB`，代码里使用 `env.DB` 访问数据库。

保存绑定后，需要重新部署一次项目。GitHub 提交新修改会自动触发部署。

## Turnstile 设置

排行榜提交需要 Turnstile。

1. 在 Cloudflare Dashboard 创建 Turnstile 站点。
2. 获得 `Site Key` 和 `Secret Key`。
3. 在 Pages 项目的环境变量中添加：

```text
TURNSTILE_SECRET_KEY = 你的 Secret Key
```

4. 在 `public/script.js` 顶部把测试 Site Key 替换成你的 Site Key：

```js
const TURNSTILE_SITE_KEY = window.TURNSTILE_SITE_KEY || "你的 Site Key";
```

注意：`Secret Key` 只能放 Cloudflare 环境变量，不能写进前端代码。

## 部署后测试

部署成功后，先测试：

```text
https://你的域名/api/health
```

正常应返回：

```json
{"ok":true,"service":"graduate-game-api","time":"..."}
```

再测试：

```text
https://你的域名/api/leaderboard
```

如果 D1 绑定正常，应返回：

```json
{"ok":true,"items":[]}
```

然后打开首页试玩。完成一局后提交排行榜，测试 Turnstile 和 D1 写入。

## 常见问题

### 部署报 Invalid database UUID

说明 GitHub 里的 `wrangler.toml` 还保留了旧的 D1 数据库 ID 占位配置。请重新上传本项目当前版本的 `wrangler.toml`。

### /api/leaderboard 报错

通常是 D1 没绑定或绑定名不是 `DB`。去 Pages 项目设置里检查 D1 绑定。

### 排行榜提交失败

检查三件事：

1. Turnstile Secret Key 是否已添加到 Pages 环境变量。
2. `public/script.js` 里的 Site Key 是否换成自己的。
3. D1 是否执行过 `schema.sql`。

### pages.dev 打不开

国内网络有时访问 `pages.dev` 不稳定。可以换网络、使用代理，或绑定自己的域名。

CREATE TABLE IF NOT EXISTS leaderboard (
  id TEXT PRIMARY KEY,
  user_id TEXT,
  guest_id TEXT,
  nickname TEXT NOT NULL,
  score INTEGER NOT NULL,
  ending TEXT NOT NULL,
  topic TEXT NOT NULL,
  difficulty TEXT NOT NULL,
  game_summary TEXT,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_leaderboard_score ON leaderboard(score DESC);
CREATE INDEX IF NOT EXISTS idx_leaderboard_topic_difficulty ON leaderboard(topic, difficulty);
CREATE INDEX IF NOT EXISTS idx_leaderboard_guest_created ON leaderboard(guest_id, created_at);

CREATE TABLE IF NOT EXISTS users (
  id TEXT PRIMARY KEY,
  email TEXT UNIQUE,
  nickname TEXT,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  last_login_at TEXT
);

CREATE TABLE IF NOT EXISTS login_codes (
  email TEXT PRIMARY KEY,
  code_hash TEXT NOT NULL,
  expires_at INTEGER NOT NULL,
  attempts INTEGER DEFAULT 0,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS sessions (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  token_hash TEXT NOT NULL,
  expires_at INTEGER NOT NULL,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_sessions_token_hash ON sessions(token_hash);
CREATE INDEX IF NOT EXISTS idx_sessions_user_id ON sessions(user_id);

CREATE TABLE IF NOT EXISTS cloud_saves (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  save_data TEXT NOT NULL,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_cloud_saves_user_id ON cloud_saves(user_id);

CREATE TABLE IF NOT EXISTS rate_limits (
  key TEXT PRIMARY KEY,
  count INTEGER NOT NULL,
  reset_at INTEGER NOT NULL
);

上传到 GitHub 前请确认：

1. GitHub 仓库根目录必须直接包含：
   - public
   - functions
   - schema.sql
   - wrangler.toml
   - README.md
   - UPLOAD_CHECKLIST.txt

2. wrangler.toml 内容必须是：

name = "graduate-game"
compatibility_date = "2026-07-05"
pages_build_output_dir = "public"

[vars]
MOCK_TURNSTILE = "false"
TURNSTILE_SITE_KEY = "1x00000000000000000000AA"

3. wrangler.toml 里面不能出现旧版 D1 数据库 ID 占位配置。

4. Cloudflare Pages 构建设置：
   - Build command 留空
   - Build output directory 填 public
   - Root directory 留空

5. D1 在 Cloudflare Pages 后台绑定：
   - 变量名称 DB
   - 数据库 graduate-game-db

6. 部署成功后测试：
   - /api/health
   - /api/leaderboard

name = "graduate-game"
compatibility_date = "2026-07-05"
pages_build_output_dir = "public"

[vars]
MOCK_TURNSTILE = "false"
TURNSTILE_SITE_KEY = "1x00000000000000000000AA"
